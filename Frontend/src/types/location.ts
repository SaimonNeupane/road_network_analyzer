export interface LocationData {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
  description: string;
  category: string;
}
export interface RadioOption {
  value: string;
  label: string;
}
